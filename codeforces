#include <bits/stdc++.h>
using namespace std;

int main() {
    int m,n;
    cin>>m>>n;
    vector<int>a(m),v;
    vector<int>b(n);
    for(int i=0;i<m;i++) cin>>a[i];
    for(int i=0;i<n;i++) cin>>b[i];
    int i=0,j=0,k=0;
    while(i<m || j<n){
            if((a[i]<b[j])){
               v.push_back(a[i]);
                i++;
            }
            if((a[i]>b[j])){
                v.push_back(b[j]);
                j++;
            }
    }for(int x:v){
        if(x!=0)
            cout<<x<<" ";
    }
    
    return 0;
}
